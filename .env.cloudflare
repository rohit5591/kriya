# The gated build: served from the Worker at the domain root, every
# recording behind a sign-in. Used by `npm run build:cf`.
#
# The plain `npm run build` ignores this file entirely and still produces
# the open GitHub Pages app under /kriya/.
VITE_GATED=true
VITE_BASE=/
